---
import logoDunkel from '~/assets/logo.svg';
import logoHell from '~/assets/logo-hell.svg';

/**
 * Wortmarke aus der Original-SVG.
 *
 * Wird als Bild eingebunden statt inline: die Datei enthält Farbverläufe mit
 * eigenen Kennungen, und bei mehrfacher Einbindung auf einer Seite (Kopf-
 * und Fußbereich) käme es sonst zu doppelten Kennungen im Dokument.
 * Die Datei ist rund 3 kB groß und wird vom Browser einmal geladen und
 * danach zwischengespeichert.
 *
 * Zwei Fassungen: auf farbigen Flächen läuft der Originalschriftzug im
 * ersten Drittel gegen zu wenig Kontrast, dort steht er deshalb in Weiß.
 */
interface Props {
  /** Höhe in Pixeln. Die Breite ergibt sich aus dem Seitenverhältnis. */
  height?: number;
  /** `hell` für farbige und dunkle Flächen. */
  ton?: 'dunkel' | 'hell';
  class?: string;
}

const { height = 28, ton = 'dunkel', class: className = '' }: Props = Astro.props;

const logo = ton === 'hell' ? logoHell : logoDunkel;
const ratio = logo.width / logo.height;
---

<img
  src={logo.src}
  width={Math.round(height * ratio)}
  height={height}
  alt="Elvora"
  class={className}
  style={`height:${height / 16}rem;width:auto`}
  loading="eager"
  decoding="async"
/>
